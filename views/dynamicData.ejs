<%# This file creates some global constants, which store on client side some CLI arguments with which the server was launched %>
globalConfig = {
	metaApiServer: '<%= metaApiServer %>',
	metaApiPassword: '<%= metaApiPassword %>'
}